
const Loader = () => (
  <div className="fixed inset-0 flex bg-gray-500 bg-opacity-25 items-center justify-center">
    <div className="w-16 h-16 border-4 border-dashed rounded-full animate-spin border-blue-500"></div>
  </div>
);

export default Loader;
